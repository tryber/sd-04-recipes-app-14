const INITIAL_STATE = {
  isShow: true,
};

const buildSearchBtnReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default buildSearchBtnReducer;
